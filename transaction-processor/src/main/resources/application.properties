server.port=8082

# kafka topic props
trans.processor.name=trans-processor

trans-approved.topic=${trans.processor.name}.approved
trans-decline.topic=${trans.processor.name}.declined
trans-refunded.topic=${trans.processor.name}.refunded
trans-billed.topic=${trans.processor.name}.billed

